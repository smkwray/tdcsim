[pytest]
addopts = -q -m "not integration and not memory_stress"
testpaths = tests
python_files = test_*.py
markers =
    integration: requires declared local raw or generated artifacts and is excluded from the default suite
    memory_stress: bounded-memory acceptance tests intended for an isolated runner
